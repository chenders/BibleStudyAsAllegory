# entries/ — the finished entries

One Markdown file per unit (e.g. `gen-22-binding-of-isaac.md`). `cain-and-abel.md`
is the worked example and the quality bar to match. Create new entries with
`/new-entry`, draft with `/draft-entry`, and validate with
`python3 scripts/validate_entry.py entries/<slug>.md`.
