# system/ — the entry layer (write *with*)

The methods and conventions you author against. Seeded here:

- `03-story-entry-template.md` — the schema every entry follows (used by the validator)
- `04-style-guide.md` — the house voice
- `02-operation-guide.md` — methods, the review loop

Add the rest of your entry-layer files here if you want them in the repo
(story index, expert-panel doc, recurring-elements registry, review logs). The
personas live as subagents in `.claude/agents/`; you can also keep the full persona
docs here for reference if you like.
